<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/edit_text_username"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="200dp"
        android:layout_centerHorizontal="true"
        android:hint="UserName"
        android:textColor="@color/colorWhite"
        android:textColorHint="@color/colorWhite"
        android:gravity="center"
        />

    <EditText
        android:id="@+id/edit_text_userPassword"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/edit_text_username"
        android:layout_centerHorizontal="true"
        android:hint="********"
        android:gravity="center"
        android:textColor="@color/colorWhite"
        android:textColorHint="@color/colorWhite"
        android:inputType="numberPassword"
        />


    <Button
        android:id="@+id/btn_login"
        android:layout_width="150dp"
        android:layout_height="40dp"
        android:layout_below="@+id/edit_text_userPassword"
        android:layout_marginTop="50dp"
        android:layout_centerHorizontal="true"
        android:text="Login"
        android:textAllCaps="false"
        android:textColor="@color/colorRed"
        android:textSize="20dp"
        android:textStyle="bold"
        android:background="@drawable/button"
        />

    <TextView
        android:id="@+id/text_view_forgotpassword"
        android:layout_width="200dp"
        android:layout_height="30dp"
        android:text="Forgot Password ?"
        android:textSize="15dp"
        android:textColor="@color/colorWhite"
        android:gravity="center"
        android:layout_below="@+id/btn_login"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="250dp" />

    <TextView
        android:id="@+id/text_view_newuser"
        android:layout_width="200dp"
        android:layout_height="30dp"
        android:text="NewUser ?"
        android:textSize="15dp"
        android:textColor="@color/colorWhite"
        android:gravity="center"
        android:layout_below="@+id/text_view_forgotpassword"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="250dp"
        android:onClick="Sinup"/>

</RelativeLayout>